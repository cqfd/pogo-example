To run locally:
* `npm install`
* `bundle install`
* `rake db:create`
* `rails s`

To run on Heroku:
* `heroku buildpacks:set https://github.com/ddollar/heroku-buildpack-multi.git`
* `git push heroku master`
